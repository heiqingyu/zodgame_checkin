name: Keepalive
on:
  schedule:
    - cron: "0 0 1 * *"
  workflow_dispatch:
permissions:
  actions: write   # 注意：API 方式需要 actions:write
jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: gautamkrishnar/keepalive-workflow@v2
